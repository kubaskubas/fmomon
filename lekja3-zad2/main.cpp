#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

int main()
{
    ifstream plik("s³owa-txt.txt");
    vector<string> wd;
    vector<vector<int>> wd2;

    while (!plik.eof())
    {

    }

    return 0;
}
